import {StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    borderWidth: 1,
    borderColor: "red",
    borderRadius: 8,
    padding: 16,
    marginBottom: 16,
    backgroundColor: "blue",
  },
});
